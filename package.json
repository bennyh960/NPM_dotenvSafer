{
  "name": "dotenv-guardian",
  "description": "guardian for your .env files that validates environment variables against a documented example file",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bennyh960/NPM_dotenvSafer.git"
  },
  "homepage": "https://github.com/bennyh960/NPM_dotenvSafer.git",
  "version": "1.1.1",
  "author": "bennyh960",
  "license": "MIT",
  "keywords": [
    "dotenv",
    "env",
    "environment",
    "validation",
    "safe-env"
  ],
  "type": "commonjs",
  "module": "./dist/index.js",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "require": "./dist/index.js",
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist/",
    "README.md",
    "LICENSE",
    "CHANGELOG.md"
  ],
  "scripts": {
    "dev": "nodemon --exec \"tsc --noEmit && tsx src/index.ts\"",
    "dev:playground": "nodemon --config nodemon.playground.json",
    "dev:fast": "nodemon --exec tsx src/index.ts",
    "build": "tsc && rimraf dist/dev_only && rimraf dist/playground.js && rimraf dist/playground.js.map && rimraf dist/playground.d.ts",
    "prepublishOnly": "npm run build",
    "build:watch": "tsc --watch",
    "release": "standard-version",
    "start": "npm run build && node dist/index.js",
    "deploy": "npm run build && node ./scripts/check-tgz.js && npm run release  && npm publish && git push",
    "deployLocal": "node ./scripts/deploy-local.js",
    "deploy:quick": "npm run build && npm version patch && npm publish",
    "test": "jest"
  },
  "devDependencies": {
    "@jest/globals": "^30.0.5",
    "@types/jest": "^30.0.0",
    "@types/node": "^24.2.1",
    "jest": "^30.0.5",
    "nodemon": "^3.1.10",
    "rimraf": "^6.0.1",
    "standard-version": "^9.5.0",
    "ts-jest": "^29.4.1",
    "ts-node": "^10.9.2",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}
